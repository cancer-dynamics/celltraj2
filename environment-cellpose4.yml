# Run from the celltraj2 repository root. Cellpose supplies PyTorch via pip.
name: cellpose4
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - cellpose==4.2.1.1
      - torch>=2.5
      - -e .[analysis,nd2]
